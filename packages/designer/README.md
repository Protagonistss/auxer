# Designer

## 规划

1. 无限画布：支持平移、缩放与物料拖拽容器（已提供基础交互，后续在此之上承载节点布局）。
2. 节点系统：抽象节点协议（ID、类型、UI/配置、连线规则），实现拖拽创建、位置记录以及层级管理。
3. 物料面板：通过 store 驱动的动态物料配置，支持搜索、分组与权限控制。
4. 属性面板：根据选中节点动态渲染表单，提供校验与双向数据绑定。
5. 历史/协同：操作栈（撤销/重做）与多端协作基础设施。
6. 数据隔离：与引擎/渲染层解耦，通过协议进行数据交换并支持快照导出。
